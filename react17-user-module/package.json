{
  "name": "react17-user-module",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "installProjects": "npm install && concurrently \"cd backend && npm install \" \"cd frontend && npm install\" \"cd portal-adapter && yarn\"",
    "start": "concurrently \"cd backend && npm run bAS \" \"cd frontend && npm start\" \"cd portal-adapter && yarn start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
